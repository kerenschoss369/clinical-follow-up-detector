# Root environment variable reference
#
# Copy each section into the matching service directory as a local .env file.
# Never commit real .env files or API keys.
#
# Day 1 (current): only the Node API section is required for local integration.
# Day 2+ (planned): LLM and database variables will be added when those features land.

# ---------------------------------------------------------------------------
# Node API — copy to apps/api/.env
# ---------------------------------------------------------------------------

# HTTP port for the Express API (default: 3000)
PORT=

# Base URL of the Python AI service (default: http://localhost:8000)
AI_SERVICE_URL=

# Maximum note length in characters (default: 20000)
MAX_NOTE_LENGTH=

# Optional ISO date (YYYY-MM-DD) used when resolving relative deadlines.
# When unset, the API uses today's date. Set this for deterministic testing.
REFERENCE_DATE=

# Timeout in milliseconds when calling the Python AI service (default: 30000)
AI_SERVICE_TIMEOUT_MS=

# ---------------------------------------------------------------------------
# React frontend — apps/web
# ---------------------------------------------------------------------------
# No environment variables are required on Day 1.
# Vite proxies /api to http://localhost:3000 during local development.

# ---------------------------------------------------------------------------
# Python AI service — apps/ai-service
# ---------------------------------------------------------------------------
# Day 1: no environment variables are required.
# The service uses a deterministic rule-based mock (no external LLM calls).

# ---------------------------------------------------------------------------
# Day 2+ / planned — not required for Day 1 integration
# ---------------------------------------------------------------------------

# LLM_API_KEY=
# LLM_MODEL=
# LLM_TIMEOUT_SECONDS=

# SQLITE_DB_PATH=
